using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ana : MonoBehaviour
{
    List<Position> positionInRange;
    [SerializeField] int healRange;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    List<Position> PositionInRange(Position position)
    {
        return null;
    }
    void HealInDistance()
    {

    }
}
